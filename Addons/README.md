# Ultroid Addons Folder

All plugin modules moved from the main deployment package are stored here.
The deployment package loader checks `addons/Addons/` first, then falls back to `addons/` for legacy flat addon repositories.
